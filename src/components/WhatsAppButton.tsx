/**
 * Os canais flutuantes (WhatsApp + central de atendimento) passaram a ser
 * renderizados pelo TicketChatProvider. Mantido como no-op para não quebrar
 * os imports existentes nas páginas.
 */
const WhatsAppButton = () => null;

export default WhatsAppButton;
